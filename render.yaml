services:
  - type: web
    name: diesel-tank-monitor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python src/dashboard.py
    envVars:
      - key: PORT
        value: 10000